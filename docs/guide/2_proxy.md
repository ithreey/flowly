# 设置代理

### Use the proxy provided by `Flowly`

The desktop app only exposes the port setting. It listens on `0.0.0.0:<port>` internally, so same-machine clients use `http://127.0.0.1:34567` and LAN devices use the Flowly machine's LAN IP, for example `http://192.168.1.10:34567`.
